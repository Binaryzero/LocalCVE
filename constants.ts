import { Cve, JobRun, Watchlist, Alert } from './types';

// Constants for the application can be defined here.
// Mock data has been removed to support real data loading.

export const DEFAULT_PAGE_SIZE = 100;
